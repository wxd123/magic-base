# 上传pypi工具安装
uv add build twine --dev

# 打包
# python -m build

# uv 打包，避免本地路径引用
uv build --no-sources


# 上传前检测
twine check dist/*

# 上传
twine upload dist/*

# 清理build文件
find . -type d -name "*.egg-info" -exec rm -rf {} + 2>/dev/null